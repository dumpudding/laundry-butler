LAUNDRY BUTLER — AUTHORITATIVE CONTEXT FOR LLMS
Updated: 2026-08-05

## Purpose

Laundry Butler is a bimanual shirt-folding project using two AgileX Piper
arms, three RGB cameras, ROS 2 Jazzy, and OpenPI pi0.5.

The immediate goal is task-specific performance on one known shirt rather
than broad garment generalization.

## Current task definition

Input state:
- one shirt;
- one size;
- one material;
- shirt begins spread on the table;
- placement is similar between episodes but naturally varies because the
  demonstrations were recorded on real hardware.

Instruction:
- use exactly: "Fold the shirt."

Success:
- the shirt is neatly folded;
- no exact final table position is required;
- arm ending positions may vary;
- both arms should finish lifted away from the shirt.

Dataset exclusions:
- exclude partial folds;
- exclude structurally incomplete recordings;
- exclude episodes marked unusable;
- do not use a "Level 2 messy shirt" initial state;
- the only valid initial-state label is "level_1_spread".

## Repository and machine paths

Repository:
  /home/laundrybutler/laundry-butler

Current raw dataset:
  /home/laundrybutler/Aloha Shared SSD/dxx_data/0727_300eps

Camera workspace:
  /home/laundrybutler/camera_ws

Piper workspace:
  /home/laundrybutler/piper_ws

Default data-collection ROS domain:
  ROS_DOMAIN_ID=88

Default inference GUI ROS domain:
  ROS_DOMAIN_ID=0

Raw datasets, MCAP files, converted LeRobot datasets, checkpoints,
training outputs, caches, and ROS build/install/log directories must not
be committed to Git.

## Current dataset status

The active dataset audit found:
- 192 active episode directories before cleanup;
- one confirmed broken episode;
- expected remaining active count after deletion: 191.

Confirmed broken episode:
  episode_20260804_154016_021_shirt-folding

Reason:
- missing /puppet/joint_left;
- missing /puppet/end_pose_left;
- missing /piper_left_ctrl_node/arm_status.

This episode must not be converted or used for training.

Approximately 130 older usable demonstrations were recorded before the
current dropcloth and lighting setup. They use the same camera angle,
shirt, procedure, and approximate trajectory, but represent a different
visual domain.

Do not blindly mix the older-lighting episodes into the first baseline.
Current plan:
1. train a baseline using the current standardized visual setup;
2. retain the older demonstrations as a separately identifiable lighting
   domain;
3. compare against a mixed run where older data are deliberately
   under-sampled rather than dominating training.

## Data recording

The unified GUI records one synchronized ROS 2 MCAP per episode with
episode and validation sidecars.

Recorded topics:

  /camera_f/color/image_raw
  /camera_f/color/camera_info
  /camera_l/color/image_raw
  /camera_l/color/camera_info
  /camera_r/color/image_raw
  /camera_r/color/camera_info
  /puppet/joint_left
  /puppet/joint_right
  /puppet/end_pose_left
  /puppet/end_pose_right
  /piper_left_ctrl_node/arm_status
  /piper_right_ctrl_node/arm_status

Do not use:

  /master/joint_left
  /master/joint_right

The authoritative training joint data come from:

  /puppet/joint_left
  /puppet/joint_right

Expected recording rates:
- cameras: approximately 30 Hz;
- arm state: approximately 200 Hz.

Topic presence in rosbag metadata is not enough. Each required topic must
have a nonzero message count and adequate rate.

Before future demonstration recording:
- adjust or lock wrist-camera exposure;
- verify both left and right joint topics;
- verify both left and right end-pose topics;
- verify both arm-status topics;
- keep the emergency stop accessible.

The wrist cameras currently brighten or overexpose as they approach the
black shirt. Do not treat this exposure shift as a reliable distance
signal. It may correlate with distance on this shirt, but it is dependent
on colour, lighting, and image composition.

## Episode timing

Do not trim the beginning of episodes.

The initial pause is intentional:
- camera exposure settles;
- the policy observes the initial shirt configuration;
- the operator identifies the pinch point before contact.

Do not trim episode endings for the current training round.

The retreat and lifted-away terminal pose are part of the demonstrated
task. Automated tail trimming may be considered later only if training
or rollout analysis shows that terminal idle frames cause a clear
problem.

## Joint representation

The physical robot representation is exactly 14 dimensions:

  0  left joint0, radians
  1  left joint1, radians
  2  left joint2, radians
  3  left joint3, radians
  4  left joint4, radians
  5  left joint5, radians
  6  left gripper opening, metres
  7  right joint0, radians
  8  right joint1, radians
  9  right joint2, radians
  10 right joint3, radians
  11 right joint4, radians
  12 right joint5, radians
  13 right gripper opening, metres

Each raw JointState message contains:

  [joint0, joint1, joint2, joint3, joint4, joint5, joint6]

joint6 is the measured gripper opening.

The combined ordering is always:
- left seven values first;
- right seven values second.

Do not silently reorder arms, interleave joints, change signs, or convert
gripper units.

## Dataset conversion

Use:

  raw ROS 2 MCAP
  -> synchronized LeRobot dataset
  -> OpenPI

Do not add an HDF5 intermediate.

The LeRobot dataset must store absolute joint values:
- observation.state: current absolute 14D robot state;
- action: future absolute 14D joint targets before OpenPI transforms.

Images:
- front camera;
- left wrist camera;
- right wrist camera.

Use episode timestamps to align camera and joint streams. Resample the
training/action grid to 30 Hz rather than storing repeated 200 Hz arm
samples as policy steps.

Normalization statistics must be calculated from the selected training
split only.

## OpenPI training decisions

Framework:
- official OpenPI;
- pi0.5;
- JAX training pipeline;
- full fine-tuning.

Do not use:
- LoRA;
- frozen model components;
- Cartesian-relative actions;
- the previous cartrel dataset or transform;
- a robot-specific ALOHA adapter without checking its semantics.

The previous Cartesian-relative pipeline produced unexplained and
incorrect behaviour. Do not reintroduce cartrel merely because older
scripts or datasets still exist.

Action representation:
- store absolute 14D actions in LeRobot;
- enable relative-joint actions inside OpenPI for the 12 arm joints;
- keep both grippers absolute.

Relative-action mask:

  indices 0-5:   relative
  index 6:       absolute gripper
  indices 7-12:  relative
  index 13:      absolute gripper

Conceptually:

  relative_arm_action =
      future_absolute_arm_position - current_arm_position

At inference, OpenPI must convert predicted relative arm actions back to
absolute joint targets before sending them to Piper.

Do not precompute relative actions in LeRobot and then apply the OpenPI
relative-action transform again.

Create a Piper-specific OpenPI data/policy configuration. The robot has
the same physical action dimension as ALOHA, but a 14D shape match does
not prove that ALOHA-specific joint or gripper adaptation is correct.

## Training scale

Training hardware:
- eight NVIDIA A800 GPUs.

Do not increase batch size simply because eight GPUs are available.

Initial global batch:
  32

Initial distribution:
  4 samples per GPU across eight GPUs

Increase to 64 only if measurements show a concrete stability or
throughput benefit.

No frozen components.
No LoRA.

The dataset is highly homogeneous, so evaluate multiple checkpoints
instead of assuming that the lowest training loss is best.

The intent is to train far enough to overfit the target task while
testing where physical rollout performance peaks.

## Action timing and inference

Training/action frequency:
  30 Hz

Predicted action horizon:
  50 steps

At 30 Hz:
  50 steps = approximately 1.67 seconds

Initial execution horizon:
  execute 20 predicted steps, then replan

Comparison:
  test executing 30 predicted steps before replanning

At 30 Hz:
  20 steps = approximately 0.67 seconds
  30 steps = approximately 1.00 second

Do not execute all 50 steps open-loop for the first tests.

No explicit command interpolation or smoothing is required for the first
rollout.

Send the 30 Hz absolute targets produced after the inverse relative-action
transform, provided the Piper command interface accepts this safely.
Inspect whether the Piper driver already performs internal interpolation
before adding another smoothing layer.

Smooth appearance is secondary to task success. Do not add smoothing if
it weakens cloth-contact timing or corrective motion.

## Evaluation

Initial evaluation uses the same shirt represented in training.

Evaluate several checkpoints physically. At minimum, compare:
- an early checkpoint;
- a middle checkpoint;
- a later, deliberately overfit checkpoint.

Record:
- whether the shirt was neatly folded;
- failed or missed pinch points;
- failed grasps;
- cloth alignment errors;
- premature stopping;
- unsafe or out-of-range commands;
- whether both arms retreat clear of the folded shirt.

No exact final shirt location or arm pose is required.

## Safety and inference requirements

Before sending policy outputs:
- restore relative arm outputs to absolute joint targets;
- preserve absolute gripper outputs;
- enforce Piper joint limits;
- enforce gripper limits;
- reject NaN or infinite actions;
- limit unsafe command discontinuities;
- provide an immediate stop mechanism.

Playback and dataset review must never publish onto live robot command
topics.

## Known failure points

1. Cartesian-relative actions previously produced unexplained behaviour.
   Do not use cartrel for this round.

2. A previous dataset/checkpoint path mixed 16D Cartesian data with a
   14D robot representation. Confirm 14D semantics, not only tensor
   shapes.

3. A rosbag can contain a topic definition while containing zero messages
   for that topic. Check message counts.

4. One confirmed episode recorded only the right arm and was excluded:
   episode_20260804_154016_021_shirt-folding.

5. Wrist-camera auto-exposure can hide shirt edges near contact.

6. ALOHA and Piper both use 14 physical dimensions, but their adapters
   must not be assumed interchangeable.

7. The OpenPI source and configuration used on the DGX are authoritative.
   A stale or extracted OpenPI copy on the recording workstation is not.

## Immediate next steps

1. Commit the repository documentation and GUI metadata changes.
2. Copy the selected raw dataset to the DGX.
3. Audit the copied dataset against the source.
4. Create and validate the MCAP-to-LeRobot converter.
5. Inspect several converted episodes visually and numerically.
6. Implement a Piper-specific OpenPI configuration.
7. Compute normalization statistics.
8. Run a data-loader/model forward-pass test.
9. Begin full pi0.5 fine-tuning.
10. Test multiple checkpoints on the physical robot.
## Physical inference rollout update — 2026-08-11

Checkpoint 15000 from the full pi0.5 fine-tune has been successfully
loaded and run end-to-end on the physical dual-Piper system.

Successful checkpoint:

  /home/laundrybutler/laundry-butler/checkpoints/
  piper_pi05_full_v1/15000

The checkpoint was verified against the DGX source before inference:
- local size: 42G;
- DGX size: 42G;
- local file count: 78;
- DGX file count: 78;
- `rsync -ani` produced no differences.

The checkpoint is an OpenPI/Orbax checkpoint containing:

  assets/
  params/
  train_state/
  _CHECKPOINT_METADATA

Its normalization statistics are stored at:

  15000/assets/local/lerobot_168_absjoint_v21/norm_stats.json


### Authoritative OpenPI inference configuration

The full-finetune run did not register its Piper config inside upstream
OpenPI's normal config registry.

Do not expect:

  scripts/serve_policy.py policy:checkpoint \
      --policy.config=pi05_piper_joint_full

to work directly against a stock OpenPI checkout.

The authoritative Piper config used for training is:

  /home/dgxtest/pi05_implementation/src/pi05_piper/openpi_config.py

Config name:

  pi05_piper_joint_full

Important configuration semantics:

  adapt_to_pi = False
  use_delta_joint_actions = True

Raw action representation:

  absolute 14-D joint position

Action layout:

  [left joint0..5,
   left gripper,
   right joint0..5,
   right gripper]

The twelve rotary joints are converted to current-state deltas internally
by OpenPI.

The two grippers remain absolute.

During inference, OpenPI's output transforms restore the rotary-joint
predictions to absolute joint targets before the robot-side client
receives them.

Do not manually apply the inverse delta transform a second time.


### OpenPI source compatibility

Before loading checkpoint 15000, the important OpenPI files on the
workstation and DGX were SHA-256 compared.

The following files matched exactly:

  pyproject.toml
  uv.lock
  scripts/serve_policy.py
  src/openpi/policies/policy_config.py
  src/openpi/training/config.py
  src/openpi/models/pi0_config.py
  src/openpi/transforms.py

Workstation OpenPI:

  /home/laundrybutler/Downloads/openpi-main

DGX OpenPI:

  /home/dgxtest/openpi

Therefore this workstation OpenPI checkout is compatible with the
checkpoint produced by the DGX run.

If these files change in the future, verify compatibility again before
assuming an old checkpoint can be restored correctly.


### Workstation Python environments

OpenPI server environment:

  /home/laundrybutler/Downloads/openpi-main/.venv

It is managed using `uv`.

`uv` was installed using Astral's user-level installer and is available
under:

  ~/.local/bin

The ROS-side inference client uses a separate Python 3.12 environment:

  /tmp/lb-ros-client

This environment was created with:

  --system-site-packages

so that it can use the system ROS 2 Jazzy Python packages while also
installing the lightweight OpenPI client package.

The OpenPI client package comes from:

  /home/laundrybutler/Downloads/openpi-main/packages/openpi-client

Do not try to run the full JAX/OpenPI policy server inside the ROS Python
environment. Keep the policy server and ROS client separated.


### GPU permission failure

A checkpoint-load attempt initially failed with:

  CUDA_ERROR_NO_DEVICE
  No visible GPU devices

The RTX 4090 and NVIDIA driver were actually healthy.

Observed behavior:

  nvidia-smi
    -> Failed to initialize NVML: Insufficient Permissions

  sudo nvidia-smi
    -> RTX 4090 visible normally

The NVIDIA device files were owned by:

  root:vglusers

and the `laundrybutler` user was not initially in `vglusers`.

Fix:

  sudo usermod -aG vglusers laundrybutler
  newgrp vglusers

After this:

  nvidia-smi

worked without sudo and:

  import jax
  print(jax.devices())

returned:

  [CudaDevice(id=0)]

Do not reinstall CUDA, JAX, or the NVIDIA driver when this exact symptom
occurs. Check `/dev/nvidia*` permissions and group membership first.

A new login should inherit `vglusers`; existing shells may require
`newgrp vglusers`.


### Checkpoint load validation

Checkpoint 15000 was successfully loaded using the exact Piper
configuration and:

  openpi.policies.policy_config.create_trained_policy(...)

Successful output included:

  Config constructed: pi05_piper_joint_full
  POLICY_LOADED

Policy metadata:

  robot: dual_piper
  raw_action_representation: absolute_joint_position
  action_layout: left_6j_gripper_right_6j_gripper
  eef_saved_in_dataset: False

This proves the checkpoint itself can be restored successfully on the
workstation GPU.


### Real-observation inference validation

Before sending any commands to the arms, inference was tested using only:
- the three real RGB camera streams;
- the real left arm JointState;
- the real right arm JointState.

No ROS command publishers existed during this test.

Required observation topics:

  /camera_f/color/image_raw
  /camera_l/color/image_raw
  /camera_r/color/image_raw
  /puppet/joint_left
  /puppet/joint_right

Required ROS domain:

  ROS_DOMAIN_ID=88

A misleading failure occurred because the parent GUI shell displayed
ROS_DOMAIN_ID=0.

The GUI launchers set domain 88 inside their child processes; exporting a
variable inside the launcher does not change the parent terminal.

Always use:

  export ROS_DOMAIN_ID=88

for the separate inference-client terminal.

The first real inference produced:

  action shape: (50, 14)
  all values finite: True

The first target differed from the measured current joint state by only
approximately:

  max rotary-joint delta = 0.00258 rad

This confirmed the observation layout, normalization, and inverse
relative-action transform were behaving consistently before physical
motion was enabled.


### CAN and Piper feedback

SocketCAN being:

  UP / ERROR-ACTIVE

does not prove that a Piper arm is actually communicating.

During setup, `can_left` was UP and ERROR-ACTIVE but had zero received
frames and the left arm produced no JointState feedback.

Check actual traffic when diagnosing this state:

  timeout 3 candump can_left
  timeout 3 candump can_right

A healthy Piper CAN interface should produce continuous frames.

The left interface recovered after restoring the physical CAN/USB
connection and bringing it back up at:

  1000000 bit/s

Do not continue policy rollout unless both:

  /puppet/joint_left
  /puppet/joint_right

are updating.


### Arm enable feedback pause

The first powered rollout aborted immediately after:

  LEFT: ENABLED
  RIGHT: ENABLED

because the freshness guard treated the temporary feedback pause during
arm enabling as a failure.

Current behavior must wait for arm/camera feedback to recover after
enabling before entering the rollout loop.

Expected startup sequence:

  Observations: OK
  Command subscribers: OK
  LEFT: ENABLED
  RIGHT: ENABLED
  Feedback recovered after enable

Only after this should REPLAN 1 begin.


### Current action execution strategy

Earlier guidance in this file says to initially execute only 20 or 30
actions from each 50-step prediction.

Those were appropriate conservative first-test settings.

Physical testing on 2026-08-11 showed that repeatedly truncating the
50-step action horizon could cause undesirable behavior resembling:

  forward motion
  -> replan
  -> partial backward/reset motion
  -> forward motion again

The current successful rollout therefore executes the complete model
horizon:

  predict 50 actions
  -> publish all 50 actions at 30 Hz
  -> obtain a fresh observation
  -> infer another 50 actions
  -> repeat

At 30 Hz:

  50 actions ~= 1.67 seconds

Current policy inference on the RTX 4090 takes approximately:

  0.09-0.12 seconds

The short inference delay between complete horizons is acceptable.

Do not perform asynchronous inference from an old observation while the
previous action chunk is still executing unless this behavior is
explicitly redesigned and tested.


### Joint discontinuity handling

Hard-aborting whenever the model produced a moderately large action step
caused otherwise successful rollouts to stop unnecessarily.

Observed raw maximum joint steps included approximately:

  0.085 rad
  0.089 rad
  0.092 rad
  0.114 rad
  0.118 rad

Instead of continually raising the abort threshold, the current rollout
uses a sequential robot-side slew limiter.

Current joint command step limit:

  0.08 rad per 30 Hz policy step

Each new target is limited relative to the previously published smoothed
target.

Example:

  raw joint step: 0.11805 rad
  published joint step: 0.08000 rad

This allows the rollout to continue without abruptly publishing the full
predicted discontinuity.

Continue to hard-abort for:
- NaN actions;
- infinite actions;
- targets outside configured Piper joint limits;
- actual arm-feedback loss.

The slew limiter is a command-safety layer and must not be confused with
changing the model's predicted action representation.


### Gripper handling

Measured Piper gripper feedback can be slightly negative when the
physical gripper is closed.

Examples observed during testing were around:

  -0.0027 m
  -0.0036 m

Do not treat these negative feedback values as meaningful negative
openings.

Before publishing policy gripper targets, clamp them to:

  [0.0, 0.08] metres

Current gripper command-step limiter:

  0.015 m per policy step

The policy still predicts absolute gripper values.


### ROS callback starvation failure

An important apparent "feedback loss" was caused by the rollout client
rather than by the Piper hardware.

Symptoms:

  left joint age  ~= 0.50 s
  right joint age ~= 0.50 s

while wrist-camera callbacks also became stale at approximately the same
time.

The front camera could remain fresh.

The original rollout node:
- used a MultiThreadedExecutor;
- left all subscriptions in the default mutually-exclusive callback
  group;
- converted every camera image through cv_bridge inside the image
  callback.

A MultiThreadedExecutor alone does not guarantee parallel execution when
callbacks are in a mutually-exclusive callback group.

The working implementation now:
- uses a ReentrantCallbackGroup;
- assigns camera and joint subscriptions to that group;
- uses six executor threads;
- keeps camera callbacks extremely cheap;
- stores only the latest ROS Image message in the camera callback;
- performs cv_bridge conversion only when constructing a policy
  observation for a replan.

After this change, the previous repeated false feedback-loss aborts
disappeared.


### Separate camera and arm freshness requirements

Do not require all camera streams to remain continuously fresh during
each 30 Hz action publication.

Current logic separates:

  observation_ready()

from:

  arms_fresh()

Before inference, require:
- front camera fresh;
- left wrist camera fresh;
- right wrist camera fresh;
- left arm state fresh;
- right arm state fresh.

During execution of an already-generated 50-step action horizon, require
only:
- left arm feedback fresh;
- right arm feedback fresh.

A temporary camera-frame delay should not abort motion that is already
being executed.

Before the next replan, wait briefly for the complete observation to
become fresh again.

Actual loss of either arm's feedback must still abort the rollout.


### Successful physical rollout

After the above fixes, checkpoint 15000 completed a continuous physical
rollout without automatic aborts.

Observed successful run:

  31 replans
  approximately 54 seconds
  50 actions per replan
  30 Hz action publication
  approximately 0.09-0.12 s inference per replan

The operator stopped the rollout manually with Ctrl+C.

Representative output:

  === REPLAN 1 | 0.0s ===
  inference: 0.106s

  ...

  === REPLAN 31 | 53.8s ===
  inference: 0.093s

This is the first confirmed full end-to-end physical inference rollout
of the trained pi0.5 Piper policy on this workstation.

The resulting behavior was qualitatively much better after:
- executing the complete 50-step horizon;
- applying the 0.08 rad sequential slew limiter;
- fixing ROS callback starvation.


### Current rollout architecture

Use four logical components:

1. Camera GUI / camera ROS nodes.

2. Arm observation/control nodes.

3. OpenPI policy server on the RTX 4090.

4. Lightweight ROS/OpenPI websocket rollout client.

The policy server and robot client communicate through the OpenPI
websocket interface.

Keep the heavyweight JAX model separate from the ROS control process.


### Remaining minor issue

Ctrl+C currently performs the intended current-position hold, but the
Python process may subsequently print a ROS shutdown exception such as:

  RCLError:
  failed to shutdown:
  rcl_shutdown already called on the given context

Earlier versions also produced:

  cannot use Destroyable because destruction was requested

These occur during process cleanup after motion has already stopped.

They are not currently evidence of inference failure or an unsafe Piper
command.

The cleanup code should eventually be changed so that:
- executor shutdown occurs once;
- the spin thread is joined;
- nodes are destroyed once;
- rclpy/context shutdown is called only if it has not already occurred.

Do not confuse this shutdown traceback with a rollout failure.


### Current inference safety requirements

For the current known-working physical rollout:

- use checkpoint 15000 unless deliberately evaluating another checkpoint;
- use the exact Piper OpenPI configuration used during training;
- use ROS_DOMAIN_ID=88;
- confirm JAX sees the RTX 4090;
- confirm both Piper CAN buses receive traffic;
- confirm both arm JointState topics are live;
- confirm all three camera topics are live before inference;
- preserve the 14-D left-first/right-second action ordering;
- use OpenPI's restored absolute joint targets directly;
- do not manually invert the relative transform again;
- clamp grippers to [0.0, 0.08] m;
- reject NaN and infinite outputs;
- enforce Piper joint limits;
- sequentially slew-limit rotary commands to 0.08 rad/step;
- execute the complete 50-action horizon at 30 Hz;
- require arm feedback throughout motion;
- require all camera + arm observations before each replan;
- keep the physical emergency stop immediately accessible.

Do not remove the hard safety checks simply because checkpoint 15000 has
produced a successful rollout.
